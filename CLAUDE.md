# CLAUDE.md

This file provides guidance to Agent tools like Claude Code when working with code in this repository.

## 项目定位

这是「商品零售管理系统」的后端，ASP.NET Core (.NET 8) Web API，用 EF Core 8 访问 Oracle 数据库。属于一个多人协作的课程项目（Vue 3 前端 + .NET 8 后端 + Oracle），后端目录是 `backend/`，与之平行的还有 `database/`（Oracle 建表与种子脚本）和 `frontend/`（前端代码仓库）。

系统涵盖五大业务模块：基础信息管理、采购进货、销售零售、库存仓储、财务分析。数据库目前共 18 张业务表。

## 常用命令

在 `backend/` 目录（含 `backend.csproj`）下执行：

```bash
# 构建
dotnet build

# 运行（默认 http profile 监听 http://localhost:5113，会自动打开 /swagger）
dotnet run

# 指定 https profile
dotnet run --launch-profile https   # https://localhost:7259
```

启动后 Swagger UI 在 `/swagger`，是接口自测的主要入口（仅 Development 环境启用）。

本项目目前**没有测试项目**，也没有配 lint 工具链。团队规范要求提交前跑 `dotnet format` 格式化。

### 数据库结构变更后重新生成实体（DB-First）

数据库是唯一事实来源（`database/01_schema.sql`）。表结构改动后，**不要手改 `Models/` 里的实体类**，而是重新 scaffold（在 `backend/` 下）：

```bash
dotnet ef dbcontext scaffold "User Id=retail_admin;Password=123456;Data Source=localhost:1521/FREEPDB1;" Oracle.EntityFrameworkCore -o Models --context-dir Data --context AppDbContext --use-database-names --no-onconfiguring -f
```

`-f` 覆盖已有文件。这是**关键架构约束**：绝不用 Code-First 迁移改库，绝不手动改共享库表结构（改动走 SQL 脚本 → DBA 审核 → 全组重新 scaffold）。

### 本地 Oracle（如需自起）

数据库跑在 Docker 里，连接信息：`localhost:1521 / FREEPDB1`，应用用户 `retail_admin / 123456`，字符集 `AL32UTF8`。`database/` 目录有 `Dockerfile` 和 `01_schema.sql`（建表）、`02_data.sql`（种子数据，含 admin/123456 等 4 个测试账号和覆盖完整业务闭环的数据）。

## 架构与约定

### 分层结构

严格分层，写新接口时照此结构：

```
Controllers/  ← 只接收请求、返回结果，不写业务逻辑
Services/     ← 业务逻辑与事务（目录目前尚未创建，需要时新建）
Dtos/         ← 对外数据结构，绝不直接把 EF 实体暴露给前端
Models/       ← EF Core 实体（scaffold 生成，勿手改）
Data/         ← AppDbContext（scaffold 生成）
```

### 统一返回格式（全组强制）

**所有接口**都返回 `{ code, message, data }` 结构，用 `Dtos/ApiResponse<T>`：

- `code`：200 成功 / 400 参数错误 / 401 未登录 / 403 无权限 / 500 服务器错误
- 分页数据的 `data` 统一用 `Dtos/PageResult<T>`：`{ list, total, page, size }`

`ProductsController.cs` 是已实现的参考样板（查询商品列表：分页 + 关键词模糊匹配 + 状态过滤），新接口照它的写法来。

### EF 实体的命名特点

实体类和属性**沿用数据库原名**（`--use-database-names`），所以是 `PRODUCT`、`SALE_ORDER`、`PRODUCT_ID` 这种全大写下划线命名，而非 C# 常规的 PascalCase。DbSet 名带复数后缀（`PRODUCTs`、`PRODUCT_CATEGORies`）。查询时用 `AsNoTracking()` 做只读查询，过滤条件要落到数据库端执行。

Schema 是 `RETAIL_ADMIN`，主键多为 `GENERATED BY DEFAULT AS IDENTITY` 自增，金额字段 `NUMBER(12,2)`/`NUMBER(10,2)`，日期字段 `DATE` 类型。`status` 列都有 CHECK 约束（如商品状态「在售」，采购单「待审批/已审批/已入库/已作废」）。

### API 设计约定

- RESTful：名词复数 + HTTP 方法，统一小写复数路由，**不加 `/api` 前缀**（如 `/products`、`/members`、`/sales`、`/points`、`/auth/login`）。Controller 用 `[Route("products")]` 这种相对路由，跨 Controller 的路径用 `[Route("~/members/{memberId}/points")]` 形式
- 业务动作用 POST，如 `purchase-orders/123/approve`
- 接口一旦在 Apifox 定稿，**不许私自改字段名/结构**，改动需走团队变更流程

### 鉴权现状

项目已引入 `Microsoft.AspNetCore.Authentication.JwtBearer` 和 `BCrypt.Net-Next`，但**尚未配置 JWT 鉴权**，当前所有接口都是匿名可访问。团队规范要求业务接口需登录（请求头 `Authorization: Bearer <token>`），登录/注册等公开接口除外。`sys_user` 密码需 BCrypt 存储。新增涉及敏感数据（如商品采购价 `PURCHASE_PRICE`）的接口时，注意这一点。后续研究后会加上JWT鉴权。

### 事务一致性（课程加分重点）

以下业务必须在 Service 层显式开启数据库事务保证原子性：

- POS 收银：扣库存 + 生成销售单 + 记库存流水 + 积分变动
- 采购入库：加库存 + 记流水 + 更新订单状态
- 退货：退库存 + 退款 + 积分冲销

### 连接串配置

连接串在 `appsettings.json` 的 `ConnectionStrings:OracleDb`，通过 `Program.cs` 的 DI 注入 DbContext（scaffold 用了 `--no-onconfiguring`，连接串不写进 DbContext）。正式部署时密码应改用 User Secrets 或环境变量，不提交真实密码。

### 本文档演进

如果发现代码事实与本文件不符，直接告诉用户，让用户决定是否更新本文件。